import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carbok.app',
  appName: 'Carbok',
  webDir: 'build',
  plugins: {
      
  },
};

export default config;